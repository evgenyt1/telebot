# Create the bot with @BotFather. Never commit the real .env file.
BOT_TOKEN=replace_me
# Generate with: python3 -c 'import secrets; print(secrets.token_urlsafe(32))'
WEBHOOK_SECRET=replace_me
TIME_ZONE=Europe/Istanbul
PORT=8080
